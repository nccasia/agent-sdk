"""Cognition domain — deliberate behavior, the reasoning work (B4).

Lobes live in ``agent_sdk.cognition.lobes`` (the domain owns its lobes, mirroring
``skills``/``memory``); the lobe network aggregates them via ``LOBES``.
"""
