"""Expression domain — the reply flow (B5).

Lobes live in ``agent_sdk.expression.lobes`` (the domain owns its lobes). Core owns
``respond``; channel/language/tone styling is the toggleable ``FormatPlugin``.
"""
